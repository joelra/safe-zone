// Root Stonecutter node. It has no buildable content of its own — the mod is
// built by the `common`, `fabric`, and `paper` branches. See stonecutter.gradle.kts
// for the active version and settings.gradle.kts for the version/branch matrix.
